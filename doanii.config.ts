// @ts-ignore
import { UiConfig } from '@doanii/ui';

const appConfig: UiConfig = {
    inputStyles: {
        container: 'my-custom-container-styles',
        input: 'my-custom-input-styles',
    },
};

export default appConfig;
